name: Runner Test

on:
  push:

jobs:
  runner-test:
    runs-on: batcave-node22

    steps:
      - name: Environment
        run: |
          echo "Batcave Runner OK"
          node --version
          corepack enable
          corepack prepare pnpm@11.20.0 --activate
          pnpm --version
